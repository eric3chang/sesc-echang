Naming Convention:

Directory_p#_c$L1_%L2.memory

where # is the number of processors [power of two], $ is the L1 cache  
size in kilobytes, and % is the L2 cache size in kilobytes [L1 cache  
size is exclusively less than L2 cache size].
